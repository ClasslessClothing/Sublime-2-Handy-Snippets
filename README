Some useful HTML/CSS snippets for Sublime 2.

Pre built template for HTML5 and CSS layout to rapidly frame a page.

Complete nav, ul, li, a menu snippet for fast menus.

CSS comments with space in between. For rapid CSS titles.